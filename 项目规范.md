# 项目开发规范

## 代码规范

* 编写前必须先规划文件职责与拆分方式，避免后期形成超长文件。
* 单个文件必须保持单一职责，禁止多个无关功能混在同一文件。
* 单文件不得超过 **300 行**，注释、空行也计入。
* 函数必须只做一件事，命名应清晰表达功能。
* 禁止重复定义相同类型、常量、函数、接口或业务逻辑。
* 内部专用实现应放入对应模块的内部目录，不应暴露给外部使用。
* 多处复用的逻辑必须抽取到共享目录，禁止复制粘贴。
* 文件层级必须清晰简洁，按功能、职责、公开/内部范围组织。

## 注释规范

* 注释必须使用中文。
* 公开函数、公开类型、公开类、公开组件必须添加文档注释。
* 文档注释必须说明功能、参数和返回值。
* 优先使用多行文档注释。
* 特殊写法、兼容逻辑、黑魔法必须在代码上方添加单行中文说明。
* 禁止无意义注释或重复描述代码本身的注释。

## Git 规范

* 修改前必须检查 git 状态，避免覆盖他人或未提交改动。
* 只允许修改与当前任务相关的文件。
* 禁止提交自己未修改或无关的文件。
* 修改后必须再次检查 git 状态。
* `.gitignore` 应排除缓存、构建产物、依赖目录和临时文件。
* 提交信息必须使用标准格式，并使用中文概要，例如 `fix: 修复登录状态判断错误`。

## AI 完成后必须自检

完成任务后必须检查并说明：

* 是否存在超过 300 行的文件。
* 是否存在重复定义。
* 是否存在过深嵌套。
* 是否存在可优化的连续条件判断。
* 是否修改了无关文件。
* 是否已检查 git 状态。
* 是否已完成 git 提交。

如果违反任意规范，必须说明原因并优先修复。